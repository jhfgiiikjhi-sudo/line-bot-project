if (user.step === "ask_name") {

  // ❌ ข้ามไม่ได้
  if (["ข้าม", "skip"].includes(lowerText)) {
    return reply(
      event,
      "❌ ขั้นตอนการตั้งชื่อไม่สามารถข้ามได้ครับ\nกรุณาพิมพ์ชื่อจริงที่ใช้เรียก"
    );
  }

  // ❌ ตัวอักษรซ้ำ
  if (/^(.)(\1)+$/.test(userText)) {
    return reply(event, "❌ กรุณาอย่าพิมพ์ตัวอักษรซ้ำ ๆ");
  }

  // ❌ มีตัวเลขหรือสัญลักษณ์
  if (!/^[ก-๙a-zA-Z\s]+$/.test(userText)) {
    return reply(event, "❌ ใช้ได้เฉพาะภาษาไทยหรืออังกฤษเท่านั้น");
  }

  // ❌ ความยาว
  if (userText.length < 2 || userText.length > 20) {
    return reply(event, "❌ กรุณาพิมพ์ชื่อความยาว 2–20 ตัวอักษร");
  }

  // ================= ไทย =================
  const hasThai = /[ก-ฮ]/.test(userText);
  const hasVowel = /[ะาิีึืุูเแโใไำ]/.test(userText);
  const startsWithVowel = /^[ะาิีึืุูเแโใไำ]/.test(userText);

  // ไทยแต่ไม่มีสระ
  if (hasThai && !hasVowel) {
    return reply(event, "❌ กรุณาพิมพ์ชื่อที่สามารถอ่านออกเสียงได้");
  }

  // สระ/วรรณยุกต์นำ
  if (hasThai && startsWithVowel) {
    return reply(event, "❌ รูปแบบชื่อไม่ถูกต้อง กรุณาพิมพ์ใหม่");
  }

  // ไม่มีพยัญชนะหลัก
  if (!/[ก-ฮa-zA-Z]/.test(userText)) {
    return reply(event, "❌ กรุณาพิมพ์ชื่อที่เป็นตัวอักษรจริง");
  }

  // ================= อังกฤษ =================
  if (/^[a-zA-Z]+$/.test(userText)) {
    const vowelCount = (userText.match(/[aeiou]/gi) || []).length;

    // ไม่มีสระ หรือสั้นเกิน
    if (vowelCount === 0 || userText.length < 4) {
      return reply(event, "❌ กรุณาพิมพ์ชื่อภาษาอังกฤษที่มีความหมาย");
    }

    // pattern สุ่มคีย์บอร์ด
    if (/^(qwerty|asdf|zxcv|werty)/i.test(userText)) {
      return reply(event, "❌ กรุณาพิมพ์ชื่อภาษาอังกฤษที่เป็นชื่อคน");
    }
  }

  // ✅ ผ่านจริง
  user.name = userText;
  user.step = "ask_age";
  saveUsers();
  return reply(
    event,
    `ยินดีที่ได้รู้จักครับ ${user.name} 😊\nคุณอายุเท่าไหร่ครับ?`
  );
}
